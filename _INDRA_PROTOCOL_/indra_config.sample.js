/**
 * INDRA SATELLITE CONFIGURATION (SAMPLE)
 * Instrucciones: Copia este archivo como 'indra_config.js' en la raíz y personalízalo.
 */
export const INDRA_CONFIG = {
  // Ruta al punto de entrada de tu lógica de negocio (Relativo a _INDRA_PROTOCOL_)
  "active_satellite": "../src/app.js", 
  
  "auto_mount": true,
  "theme": "dark",
  "debug": true
};
